---
title: 配置总览
description: AICodeReviewer 配置的组织方式与覆盖层次。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将介绍顶层配置命名空间（`llm`、`triggers`、`workspaces`、
`outputs`、`agent`、`review`、`queue`、`storage`、`compression`），

全局 → workspace 默认 → workspace 实例的覆盖层次，以及
`.env` 与 `config.yaml` 的 secret 约定。
