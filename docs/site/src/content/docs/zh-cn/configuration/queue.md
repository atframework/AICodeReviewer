---
title: 队列与重试
description: 配置持久化队列、worker、速率限制和重试策略。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将介绍 `queue.kind`（memory / sqlite）、`queue.workers`、
`queue.rate_limit`、`queue.retry`（规范字段 `attempts` 与 `backoff`）以及
`queue.dead_letter`。
