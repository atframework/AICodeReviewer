---
title: Agent 与沙箱
description: 选择 agent CLI，配置超时、上下文压缩和沙箱后端。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将介绍 `agent.default`（kilo / opencode / zoo / claude-code /
copilot-cli）、`agent.timeout_seconds` 与整棵进程树清理、
`agent.context_compaction`，以及沙箱后端
（native / docker / podman / docker_socket）。
