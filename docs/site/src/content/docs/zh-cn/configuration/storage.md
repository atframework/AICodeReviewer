---
title: 存储
description: 配置数据库、缓存和对象存储后端。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将介绍顶层 `storage` 命名空间：
`storage.database`（默认 sqlite，postgres 预留）、`storage.cache`
（memory / redis / none）、`storage.object`（filesystem / s3）以及保留策略。
