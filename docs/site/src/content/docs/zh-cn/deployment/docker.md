---
title: Docker 部署
description: 使用 Docker 构建并运行 AICodeReviewer 单容器镜像。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将介绍不使用 Compose 的 `docker build` / `docker run`、
卷布局（`config.yaml`、`data`、`workspaces`、`logs`）、`docker_socket`
沙箱后端、嵌套容器沙箱，以及运行时镜像的工具基线。
