---
title: CLI 命令
description: aicr CLI 子命令与参数参考。
sidebar:
  badge:
    text: 待完善
    variant: caution
---

本页为占位页。将列出每个 CLI 子命令（`serve`、`review`、`eval`、`replay`、
`memory`、`lint`、`doctor`）、每个参数，以及每个命令的可运行示例。从
`packages/cli/src/app.ts` 的 help 文本生成。
