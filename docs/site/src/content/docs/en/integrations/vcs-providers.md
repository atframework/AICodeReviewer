---
title: VCS Providers
description: Integrate GitHub, Gitea/Forgejo, GitLab, Perforce (P4), and Subversion (SVN).
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will document each VCS provider: webhook routes
(`/webhooks/{gitea,forgejo,github,gitlab}`), the P4 and SVN trigger endpoints
(`/triggers/{p4,svn}`), per-kind secret mechanisms, file-filter globs, and the
managed-issue / PR-review channel pairings per provider.
