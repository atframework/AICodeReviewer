---
title: Docker Deployment
description: Build and run the AICodeReviewer single-container image with Docker.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will cover `docker build` / `docker run` without
Compose, volume layout (`config.yaml`, `data`, `workspaces`, `logs`), the
`docker_socket` sandbox backend, the nested-container sandbox, and the runtime
image tool baseline.
