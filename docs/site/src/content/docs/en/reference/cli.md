---
title: CLI Commands
description: Reference for the aicr CLI subcommands and flags.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will list every CLI subcommand (`serve`,
`review`, `eval`, `replay`, `memory`, `lint`, `doctor`), each flag, and a
runnable example per command. Generated from the help text in
`packages/cli/src/app.ts`.
