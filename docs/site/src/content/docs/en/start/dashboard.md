---
title: Dashboard and Logs
description: Use the observability dashboard, /metrics, and run snapshots.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will cover the built-in dashboard (`/dashboard`),
the Prometheus metrics endpoint (`/metrics`), the admin login model, and where
run logs / snapshots live on disk.
