---
title: Configuration Overview
description: How AICodeReviewer configuration is organized and layered.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will introduce the top-level config namespaces
(`llm`, `triggers`, `workspaces`, `outputs`, `agent`, `review`, `queue`,
`storage`, `compression`), the global → workspace-default → workspace-instance
override layering, and the `.env` vs `config.yaml` secret convention.
