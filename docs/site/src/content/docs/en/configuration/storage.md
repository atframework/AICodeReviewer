---
title: Storage
description: Configure the database, cache, and object storage backends.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will document the top-level `storage` namespace:
`storage.database` (sqlite default, postgres reserved), `storage.cache`
(memory / redis / none), `storage.object` (filesystem / s3), and retention
settings.
