---
title: Queue and Retry
description: Configure the durable queue, workers, rate limits, and retry policy.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will document `queue.kind` (memory / sqlite),
`queue.workers`, `queue.rate_limit`, `queue.retry` (the canonical `attempts`
and `backoff` fields), and `queue.dead_letter`.
