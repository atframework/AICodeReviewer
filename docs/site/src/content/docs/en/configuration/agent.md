---
title: Agent and Sandbox
description: Choose an agent CLI, configure timeouts, context compaction, and the sandbox backend.
sidebar:
  badge:
    text: WIP
    variant: caution
---

This page is a placeholder. It will cover `agent.default` (kilo / opencode /
zoo / claude-code / copilot-cli), `agent.timeout_seconds` and whole-process-tree
cleanup, `agent.context_compaction`, and the sandbox backends
(native / docker / podman / docker_socket).
