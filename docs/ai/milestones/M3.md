# M3 — 压缩、Scrubber、Fallback、预算、Markdown 修复、Redis 队列（已完成）

两阶段 diff 压缩、secret scrubber、LLM fallback/限流/预算、Markdown 修复与
BullMQ Redis 队列已交付。

历史验收覆盖大 diff、429 恢复、并发去重及真实 Redis；原记录未保存独立执行计数。
实现入口为 core 的压缩/队列/限流模块与 llm gateway；
稳定合同见[架构](../architecture.md) §3.3–3.5、§3.10–3.11。
