# M17：配置存储、schema 迁移与 PostgreSQL 业务后端(P2)

2026-09-12 交付 ConfigStore 四后端合同、checksummed 迁移账本、migrate CLI、
持久管理 session 与 PostgreSQL 业务 StoreDb。稳定合同见[架构](../architecture.md) §3.14。

证据：config-store-conformance、各后端 store、migration-runner、
store-migration-fixture 与 CLI migrate 测试。
Windows + WSL PG 17.11/Redis 的全量 coverage 为 133 文件、3188 通过/6 条件跳过；
lint/typecheck/Markdown/build/eval 通过。运行时/API/UI 后续由 M19–M20 接线，
指定历史版本进程矩阵由 [M24](M24.md)补齐，不能继续列作未实现。

真实 PG 低权限用例要求测试角色具备 CREATE ROLE。Redis 必须使用独立实例或自有前缀清理，
禁止 FLUSHDB；原共享实例清库建议已失效，以[验证基线](../AGENTS.repository-baseline.md)为准。
