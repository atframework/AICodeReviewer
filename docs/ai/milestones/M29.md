# M29 计划精简与 WSL 临时服务验收（2026-09-22）

32 份原里程碑由 170407 字节精简到 25600 字节，保留交付、验证与限制；
修正 M17 清库建议、M18–M23 阶段性待办和 M11 配置示例校验状态。
M28 原始字段门禁失败仍保留；Plan 不再复述完成史，补回 M14 辅助仓库验收缺口。

新增 tests/services/with-gitea.sh 与 outputs/test/gitea-assignment-live.test.ts：
WSL2 Debian 13.6 / rootless Podman 5.4.2，固定 Gitea 1.25.4 + SQLite，
1 CPU / 512 MiB、回环随机端口、900 秒运行上限，合成用户/私有仓库。
实际调用发布器并 GET issue 核验 assignees，覆盖三种 issue 模式、未关联邮箱、
只读发布者、不存在用户及存在但无仓库权限的用户。

临时目录位于 ~/workspace/github/atframework/aicr-acceptance.<随机值>/build/tmp/；
脚本退出时只清理本轮容器、目录及原先不存在的镜像。
版本/digest/资源及门禁日志在 build/logs/gitea-*、build/logs/roadmap-final-*。
初次 tmpfs uid 参数被本机 Podman 拒绝，清理后改为 keep-id 独立目录；没有改变资源上限。
操作与可行性依据见[服务验收](../../testing-services.md)。

双语贡献说明、示例入口、验证基线和 plan-implementation-audit skill 同步。
未改产品配置、运行时 prompt 或客户端桥接：本次改变验收工具与文档，不改变评审协议。
固定版本 Gitea 结果不证明 GitHub/Forgejo、生产凭据、GitLab 或付费 LLM 验收；
GitLab 按官方资源要求单独安排，未启动。

验证：Windows Node 24 完整基线通过，195 个测试文件与源码清单一致，4562 项全部通过，
零失败/跳过；Gitea 7 项、真实 PG/普通 Redis/独立 OOM/SVN/P4 均实际执行。
coverage 为 statements 90.53%、branches 83.15%、functions 92.17%、lines 91.90%，
范式模块阈值保持不变。lint、类型检查、新测试严格类型检查、270 文件 Markdown、
runtime build、6 项离线 eval、54 页双语 docs:build 和 170 个本地链接检查通过。
本轮未触及 UI/配置 API，浏览器门禁不适用；未执行全量原生 Linux runtime 门禁。

成功路径及显式失败命令（退出 1）均清理了 Gitea 容器、目录和本轮镜像；
原有停止的 Redis 容器及四个宿主缓存镜像保留。Windows 临时 PG/Redis 退出且端口关闭后，
也删除了本轮 cluster/数据目录，只保留 build/logs 日志。Gitea 运行中采样约 93–109 MB，
不是峰值测量。异常强杀仍按服务指南核对数据残留。
