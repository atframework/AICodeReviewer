# M20：配置管理表单与管理页面（P6）交付

## 状态

2026-09-14 交付 P6，并补充真实 registry、API 持久化和浏览器组合审查。
U01–U24 参数化覆盖由组合回归补强，三个范式模块继续强制 100% 四项覆盖率。
设计合同见
[详细设计](../../superpowers/specs/2026-09-11-workspace-config-management.md) §8，
测试 ID 证据映射见
[测试计划](../../superpowers/plans/2026-09-11-workspace-config-tests.md)。
P7/P8 保留在执行计划与 Plan.md；本次同步已改变的架构、双语操作说明和示例。

## 交付内容

| 层 | 模块 | 合同 |
| --- | --- | --- |
| core 范式 | `config-ui-spec.ts` | ConfigUiSpec 类型、由组件 schema/字段清单构建的 registry、`validateUiSpec`；枚举/默认值引用 schema 定义，不遍历 Zod 私有结构（U01–U03、U24） |
| core 范式 | `config-ui-runtime.ts` | `decodeDraft`/`encodeChanges` 往返（U17/U18）、`resolveFieldState`、`resolveOptions`/`resolveItemOptions`（U19）、`parseNumberInput`（U06）、map key 转义（U09）、visibleWhen（U13） |
| core 范式 | `config-form-state.ts` | 编辑器 session 状态机：创建/改值/kind 切换（U14/U15）、dirty 基线跟踪、changeset 编码、API 错误映射（U20） |
| server 客户端 | `dashboard/client/{config-app,renderer,api-client}.js` | 通用 renderer + API client 从内联 HTML 分离为可测试 ES module；无 React/新 UI workspace；动态文本一律 textContent（A14） |
| server 服务 | `dashboard/index.ts`、`scripts/copy-assets.cjs` | 客户端资产从 dist 按白名单名服务；core 编译产物（config-ui-runtime/config-form-state/weekly-schedule）由 build 复制 |
| server API | `config-api.ts` | `GET /options/:source` 动态选项（实体 id：文件 + 数据库记录）；schema 响应携带 uiSpec |
| 浏览器门禁 | `tests/browser/`、Playwright | 真实 server fixture（SQLite 配置后端、文件锁字段、无外部 LLM/生产 DB）；`pnpm test:browser`；CI `Dashboard browser gate (P6)` job |

页面：providers、模型组/覆盖、triggers、channels、routing（含 route
preview）、agent/search/sandbox、review、workspace（含 matcher、
path-template、继承/覆盖显式状态）、版本历史（列表、redacted diff 详情、
restore）。保存显示实际 revision；409 冲突保留草稿并给出差异比较；
文件来源只读 + "复制为新数据库配置"。

## 本轮修复（浏览器门禁首跑暴露）

| 问题 | 修复与回归证据 |
| --- | --- |
| ordered-list 行内动态选项（模型组 entry 的 provider select）为空 | `renderNestedItemField` 只取静态 `itemField.options`；新增 core `resolveItemOptions`（U19 的行字段变体），renderer 注入 `setItemOptionsResolver`，行值消失仍保留为禁用选项；config-ui-runtime.test.ts 直测 |
| 保存后整页重建折叠所有 section，focus/aria 树断链 | open 状态按上下文（drawer/page）持久化到 `state.sectionStates`，renderDrawer/renderGlobalsFields 先捕后建再恢复；浏览器继承往返用例覆盖 |
| spec 断言对 input 值不可见（textContent 不含 value） | 改为 `toHaveValue`/aria-pressed 断言；XSS 用例落到可自由文本的 `review:labels.auto_tag`，断言 payload 以 input value 往返、`__xss` 未执行、无 img 节点 |
| 嵌套 map 控件的 "＋ Add" 与列表自身 Add 歧义 | 浏览器用例按 aria-label（`Add Model entries row`）精确定位 |
| 版本表断言固定行数，但 namespace 在同一 fixture 进程内累积 revision | 断言改为相对 head revision 与首行最新；审计详情断言与 S06 脱敏合同一致（实体 id，非字段路径） |
| `auto-commit-webhook.test.ts` 两个 P4 用例在装有真实 p4 的机器上超时 5 s | `enrichP4ReviewEvent` 默认 runner 真实 spawn `p4 describe`，假主机名走 TCP 连接超时约 22 s；fixture 端口改 `127.0.0.1:1` 立拒；教训写入 `docs/ai/pitfalls/AGENTS.vcs.md` |
| `config-form-state.ts` else 分支（实体无 kindOptions 时新草稿不默认 kind）未覆盖 | config-form-state.test.ts 新增正反用例；100% 分支阈值恢复 |

## 初次门禁（审查前）

- `vitest run --coverage`：149 个测试文件、3579 项通过、35 项跳过
  （跳过均为无本机服务的 PostgreSQL/Redis/live-VCS 套件；其证据见
  M17–M19，本轮未重新提供环境）。`config-ui-runtime.ts`、
  `config-ui-spec.ts`、`config-form-state.ts` 100% statements/branches/
  functions/lines 阈值通过（vitest.config.ts 按文件强制）。
- `pnpm test:browser`（根 `playwright.config.ts`）：11/11 通过——desktop
  project 全量 10 例（导航与状态栏、文件只读 + copy-as-new、provider CRUD
  立即生效、409 冲突草稿保留与 rebase 重试、workspace 继承/覆盖往返、
  XSS 惰性渲染、窄屏布局、键盘 Alt+Arrow 排序、route preview、版本
  列表/restore），narrow project 420px 跑只读响应式用例。两 project 共享
  同一有状态 fixture namespace，实体写入用例只在 desktop 运行，避免跨
  project 的 ID 冲突（首跑窄屏复跑 CRUD 时实测 409）。
- ESLint（0 warnings）、全仓 `tsc -b`、markdownlint 125 文件、
  11 包 `pnpm build`、`eval --validate-only` 全部通过。

## 代码审查修复

初次纯函数和浏览器门禁没有覆盖以下组合，不能单凭覆盖率认定行为完整。

| 问题 | 修复与回归 |
| --- | --- |
| registry 将 `[]`/`*` 压平成普通路径，保存丢失 match、周计划或未知后代 | 自动构建递归列表/map 控件；相对路径编码保留扩展、显式空对象和数组；真实 registry 往返与双窗口/双星期浏览器持久化 |
| 模型组行将 UI ID 写入配置、连续选择丢失前一次值 | 行值按配置路径读写；嵌套编辑更新当前行；断言 API 返回的 model、overrides.seed 和 schedule 原值 |
| kind 切换/重基丢值，删除后重基重新创建，改名未编码 rename | 保留共同字段和未改变 kind 的原值；缺失记录拒绝重基；rename/update 同 changeset；core 组合回归 |
| 跨页暂存未接入，选项与路由预览只读已发布配置 | 暂存 provider/group 等引用后原子发布；预览携带暂存操作复用 prepare，拒绝过期基线并断言无 revision/audit 写入 |
| 分页截断配置，其他页保存推进脏草稿的 CAS 基线 | 遍历 nextOffset 并检查 head/digest；保存用草稿原基线；API client 与跨页冲突浏览器回归 |
| POST 响应丢失、202、restore 恢复可能重复提交或丢状态 | 保存期间锁定编辑；先查 operation，显式重试保留完整原请求；刷新失败与提交结果分开；浏览器覆盖 changeset/restore 响应丢失与 202 |
| 无效数值保留旧草稿值，保存错误被重绘覆盖 | 阻止带本地输入错误的保存/暂存；重绘后显示服务器错误；浏览器拒绝/修正回归 |
| 模板补全插入单花括号，缺少 helper、可空兜底与禁用变量过滤 | 变量注册表生成 segment/default 补全文本；浏览器补全进入真实暂存预览 |
| 未使用的授权 env 无法选择，继承的 options 属性可被调用，脱敏占位符回写 | 限定自身 options 源、授权 env 名；占位符拒绝持久化；API 阴性用例 |
| 再次打开 Config 标签时 import Promise 被模块覆盖；缺失 core 资产仍构建成功 | 去掉模块全局副作用，缺失资产显式失败；浏览器重开与正式构建门禁 |

新增 `config-ui-integration.test.ts`、`config-ui-client.test.ts`；扩展现有 API 和
浏览器测试。现有 100% 阈值、测试收集和外部服务跳过策略保持不变。
最终完整门禁：151 个测试文件通过，3606 项通过、35 项按外部服务条件跳过；
相较初次门禁新增 27 项单元/接口回归。三个范式模块的 HTML 覆盖率报告均为四项
100%。浏览器 21/21（新增 10 项），ESLint、tsc、markdownlint（126 文件）、
11 包 build、6 个 eval fixture、docs:build 与 docs:check 均通过。
docs:check 为 0 errors、0 warnings、11 条既有 hint；站点构建仍提示缺少独立 404
内容条目，不影响 54 页生成。运行日志位于 `build/logs/p6-final-*`。

沙箱首跑出现既有 fixture 写入 `D:/tmp` 的 EPERM、进程树清理限制，以及 pnpm
启动器数据库打不开；完整测试、构建与浏览器门禁已在主机环境用原参数复跑通过。
版本表回归按 revision 单元格精确断言，避免双位 revision 与相邻 parent 文本拼接误判。

## 边界

- 管理流程的真实多后端（PostgreSQL/Redis）发布与多副本一致性属 P7；
  本轮浏览器证据使用 SQLite 配置后端单进程 fixture。
- 已同步 Dashboard 操作说明、架构 §3.16、example README、Plan 和 config/state
  提示；配置声明与运行时 review prompt 没有新参数，未添加重复 skill 或桥接规则。
  P8 的全链文档收敛仍未完成。
- 浏览器门禁在 CI 以 Chromium headless 运行；未覆盖 WebKit/Firefox。
