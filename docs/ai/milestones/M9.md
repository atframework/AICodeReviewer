# M9 — 发布收尾（基本完成）

## 范围

- 文档、示例与部署资产收束。
- 容器嵌套沙箱验证。
- Dockerfile 与部署脚本修复。
- 从零部署验收。

## 主要交付

- `docker_socket` 文档确认：`example/README.md` 和 `docs/ai/architecture.md` §3.8.1 已补充说明
- 容器嵌套沙箱验证：`AICodeReviewerTest` 测试环境验证通过（Podman socket + Docker 静态二进制 + `--userns=keep-id --group-add keep-groups`）
- `k8s_pod` / `firecracker` 平台能力边界说明：`architecture.md` §3.8.1 后端能力矩阵已标注为预留扩展位
- 版本固定与 changelog：`CHANGELOG.md` 已创建，所有包版本固定为 `0.1.0`
- 最终发布检查单：`docs/ai/milestones/M9-checklist.md`
- `deploy.sh` 修复：所有 `podman` 命令通过条件化 engine 参数加 `--storage-driver=overlay`，Docker engine 不接收 Podman 专用参数；加入 preflight 检查
- `deploy.sh` 硬编码路径：改为环境变量覆盖 `AICR_DEPLOY_DIR`/`AICR_IMAGE_NAME`/`AICR_HOST_PORT`/`AICR_CONTAINER_NAME`/`AICR_ENGINE`
- `deploy/docker-static` 清洁同步兼容：未启用嵌套沙箱时创建空占位文件；启用时下载真实 Docker CLI
- `.dockerignore`：排除 `.git`/`node_modules`/`dist`/`coverage`/`docs` 等
- Dockerfile 前向兼容：补齐 `sandbox`/`eval` node_modules COPY
- 部署资产审计修复：P4 认证表修正、`.env.sample` 补齐缺失变量、`config.yaml` 补齐 queue 子项示例、`SKILL.md` 环境变量名对齐、`Caddyfile.example` 添加
- 增量部署验证：测试环境源码同步 → `deploy.sh` 构建 → 启动 → `healthz` / `/metrics` 验证通过
- 从零部署验收：`AICodeReviewerTest` 空目录完整验收通过（`rm -rf` → `mkdir` → 解压源码 → 写入 config.yaml/.env → `deploy.sh` 构建 → `healthz` OK）
- 容器嵌套沙箱集成验证：Docker CLI v27.5.1 在 AICR 容器内正常运行；嵌套 `docker run --rm alpine echo sandbox-ok` 成功；`--network none` 网络隔离验证通过

## 验收沉淀

- 单容器自托管路径完整可用：源码 → 构建 → 启动 → 健康检查通过。
- 嵌套沙箱在 Podman rootless 环境下验证通过。
- 所有 lint、typecheck、测试（1228）与 markdownlint 均通过。

## 当前稳定落点

- 部署与验收入口：`../../example/README.md`、`../../docs/podman.md`
- 发布检查单：`M9-checklist.md`
- 沙箱后端能力矩阵：`../architecture.md` §3.8.1

## 遗留项

- 版本 bump 与 git tag（用户决策）

## 对后续阶段的影响

- M7 及后续迭代继续使用 M9 确立的部署合同与发布流程。
