# M31 验收范围收敛与 WSL 部署验证（2026-09-22）

按用户本轮指定标准关闭以下验收；沿用 [M30](M30.md) 的 P4/GitHub 用户确认，
不重复生产发布、飞书通知或付费 LLM 请求。

| 验收 | 证据与边界 |
| --- | --- |
| 飞书 | M30 的真实发送/撤回，加用户观察确认；跨群、重名、外部成员不纳入本次验收 |
| SVN | Podman 真仓库→diff/取文件→orchestrator→确定性模型→发布断言通过；辅助仓库物化测试同时通过。按用户标准不再要求 agent 容器实际只读挂载 |
| 跨 workspace、重启、保留与重置 | scheduler、execution-concurrency、runtime-config、history-retention、observability、shutdown 回归；数据明细删除保持汇总/rollup/幂等事实的断言继续保留 |
| 数据库凭据 | 新增独立 SQLite 连接的多副本读取、主密钥轮换、关闭重开数据库/队列、旧任务取原凭据，以及缺退役密钥/不一致副本拒绝执行的组合回归 |
| LLM CLI、套餐与限流恢复 | agents、provider-presets、gateway、rate-limiter、review-orchestrator 的参数/环境、端点选择、额度分类与恢复测试；按用户允许的单元测试标准关闭，不声称实测供应商扣费 |
| 其他升级与旧实例排空 | migration-version-process、migration-runner、runtime-config、server-shutdown 和 serve-shutdown-process 自动化验收；保留 M24 固定历史源码与当前驱动的适用范围，不承诺所有旧发行版兼容 |
| CI 真实 LLM 质量 benchmark | 用户明确跳过；离线 eval 合同校验继续运行，未取得质量分数 |

WSL Debian 的部署夹具验证 PG SCRAM/TLS/角色、Redis TLS/ACL/AOF、SVN HTTPS/authz，
并核验真实 post-commit hook 的 www-data 身份及 PATH。写入后停止/启动容器，
三种服务的持久化读取通过。共享 1 CPU / 512 MiB / 128 PID，命名卷和随机回环端口；
临时目录位于 ~/workspace/github/atframework 的独立子目录。入口与版本规则见
[服务指南](../../testing-services.md)。准备期曾失败：initdb 降权后无法读取 /dev/stdin，
改用服务用户可读的临时密码文件，初始化后删除；首次失败日志保留。

真实 TLS/ACL 端点暴露 Redis queue/auto-commit 手写 URL 解析丢失 TLS、用户名和
密码解码的缺陷；CLI Redis 启动超时。改为向 BullMQ/ioredis 传递完整 URL，
新增参数回归，保留 red/green 日志；不能把首次中断的全量门禁称为通过。
另修复迁移消费夹具在 sslmode 后拼接第二个问号导致 schema 参数丢失的问题；
改用 URL.searchParams，保留 TLS。该轮全量为 4559 通过、1 失败、14 跳过。

18:18 公网只读核查：systemd active/running、Restart=always、健康端点正常；1886 条运行记录，
428 成功、1329 跳过、129 历史失败；跨 workspace 历史区间有 179 对重叠。
182 条 Events 含 queued/timeout 等决策，汇总随自然任务增加；本次启动后已有
1 次成功和 4 次跳过。自动批次有 232 completed、2 running、6 skipped，
完成记录的 recovery_attempt 最大为 1；运行中租约无过期。
当前一条实例登记、零配置发布、两份快照且无密文，未配置主密钥/退役密钥。
因此生产状态仅作现状证据，重启恢复、保留策略重置和密钥轮换采用自动化测试证据；
没有重启生产、改配置、写数据或触发评审。容器最近 24h 上限 2000 行抽样实际 20 行，
未匹配错误关键词；另查 aicr.log 尾部 64 KiB，有 429/timeout/error 历史记录，
未匹配 snapshot_invalid 或 secrets_key_missing。没有把日志抽样当作历史零错误证明。

专项验证：凭据文件 36 项通过；调度/保留/限流/模型合同 6 文件 170 项通过；
SVN 网络物化与分析 2 项通过。最终全量发现 199 个文件、4574 项测试，
4560 通过、14 按环境条件跳过、零失败；跳过项为本轮不重复执行的 P4、Gitea、
飞书与真实 LLM。覆盖率 statements 90.60%、branches 83.29%、functions 92.22%、
lines 91.97%，原门槛全部通过；lint、typecheck、build、6 个离线 eval 夹具、
Markdown 与 docs:build 通过。没有涉及 dashboard/browser 合同，不适用浏览器门禁。

测试进程运行于 Windows Node 24.21.0，服务运行于 WSL/Debian；本轮服务版本为
PostgreSQL 17.11、Redis 8.0.2、Apache 2.4.68、SVN 1.14.5。
退出后独立复查：自有容器、命名卷、WSL 临时目录与原生 SVN 夹具均已删除，
五个临时服务端口均拒绝连接；保留原有停止容器及四个缓存镜像，没有残留临时服务。
日志：build/logs/acceptance-current-*、deployment-*、svn-analysis-*、production-acceptance-detail.log。

同步服务指南、双语开发/队列文档、配置示例与 AI 验证指引。评审协议和 agent 指令未变，
无需修改评审 prompt、agent skill 或客户端桥接。剩余开发与 GitLab 验收已在 [M34](M34.md) 收口。
