# M30 环境变量验收与 SVN 物化修复（2026-09-22）

用户确认 P4、GitHub 集成已在公网正式环境通过；据此关闭对应验收项，本轮不重复。
Gitea 1.25.4 复用 [M29](M29.md) 的临时服务与七项真实指派用例，本轮全量回归重新执行。

真实服务测试按环境变量启用，缺省跳过、部分配置失败；显式调用
tests/services/with-local-secrets.mjs 才读取 development/secret/secret.yaml。
只向测试子进程传值，不写入配置或日志；操作见[服务指南](../../testing-services.md)。

- 飞书：指定租户的群成员/资料读取、实际 dispatcher 发送 JSON 2.0 卡片、撤回通过。
  不输出成员资料；没有指定真实 @ 对象，没有向 LLM 发送目录。
- LLM：zhipu 的 glm-5.3-flash、kimi_for_coding 的 kimi-for-coding，各通过 OpenAI
  与 Anthropic 直接客户端短请求及 usage 检查。每例最多一次、256 输出 token、60 秒，
  无自动重试；不是评审质量、全部 agent CLI 或套餐限流验收。
- SVN：固定 Debian digest + Subversion 1.14.5-3，rootless Podman，1 CPU / 256 MiB，
  回环随机端口，900 秒上限。真实辅助仓库用例先复现 E155000；修复空目录导出，
  解析 HEAD 后固定 revision，每次重试清除部分导出。固定版本/HEAD 内容、diff、
  失败和过期 alias 清理通过；失败与成功退出均移除自有容器及目录。
  运行中采样约 1.5 MB，不是峰值；agent 容器实际只读挂载仍列 Plan。

全量覆盖率发现 198 个测试文件：4563 通过、7 跳过、零失败。跳过为用户已确认的
P4 真服务 3 项及单独执行的飞书/LLM 4 项；Gitea 7 项与 SVN 网络物化实际运行。
coverage 为 statements 90.56%、branches 83.19%、functions 92.22%、lines 91.94%，
原有阈值保持不变。日志保留在 `build/logs/live-*` 与 `build/logs/acceptance-final-*`。

Windows Node 24.21.0：lint、构建类型检查及新增测试严格类型检查通过，
271 文件 Markdown、runtime build、6 个离线 eval、54 页双语 docs:build、
本地文档链接/skill metadata 与 diff 检查通过；未改 UI/配置 API，浏览器门禁不适用。
三组变量不完整的阴性检查均在联网前失败。WSL 承载服务，测试进程使用 Windows Node；
没有把本轮结果描述为全量原生 Linux runtime 验收。

最终核对无本轮容器、数据目录或新拉取的 Gitea 镜像；原有停止的 Redis 容器与
四个缓存镜像保留。本机临时 PG/Redis 停止并删除 cluster；三个本轮 SVN fixture
目录也在确认无对应 svnserve 后删除。飞书测试卡片已撤回。
同步了双语开发/配置说明、示例、源码依据、pitfall 与计划审计 skill；评审提示词和
客户端桥接无需变化，因为评审协议与 agent 指令没有改变。
