# M1 — Gitea + Git + 单 LLM 最小闭环（已完成）

Gitea webhook → ReviewEvent → Git scoped fetch → LLM → PR 行评与摘要的最小闭环已交付，
含 CLI serve/review --dry-run 和 MCP 输出收集器。

原验收口径为测试环境 webhook 与 PR 发布闭环，未保存独立执行计数。
稳定合同见[架构](../architecture.md) §3.1、§3.2、§3.9。
