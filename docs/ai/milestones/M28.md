# M28 自动批次逐目标发布恢复与配置示例扩展校验（2026-09-21）

2026-09-21 交付逐渠道 publication_pending 回执续发与文档 YAML 配置片段全量校验。
有效恢复跳过 LLM/已确认渠道；回执即时持久化、租约失败中止后续发布；Redis checkpoint
保存不透明 JSON，载荷/回执超限时清除旧部分状态，保留原模型用量。
稳定合同见[架构](../architecture.md) §3.1.1、[输出合同](../../output-channels.md)、D50。

证据：auto-commit-runtime/store-conformance、review-orchestrator、bootstrap、
observability-api、config-examples；复审先复现 9 项失败后修复。
配置检查发现全部文档的候选 YAML，畸形/未知键不静默跳过，并含 7 个阴性场景。

原始门禁：4421 通过 / 1 失败 / 8 跳过，失败为同期 Feishu schema 字段清单 U24；
docs:check/build 内嵌字段检查同样失败。lint/typecheck、266 文件 Markdown、runtime build
与直接 Astro check/build 通过，不能合称完整基线通过。
日志 build/logs/plan-review-{red,focused,stores,final-*}。

远端发布与本地 checkpoint 不同事务，unknown 自动重发仍可能重复；
远端对账协议后续交付见 [M33](M33.md)。
