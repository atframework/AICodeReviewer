# M33 自动批次远端对账（2026-09-22）

逐 HTTP 报告写入保存操作身份、状态与写入/对账次数；Git 查询正文标记和原资源，飞书应用
使用有时限的 UUID，未知 webhook 停止重发。SQLite/Redis/内存的人工 Retry 保留远端日志，
日志超限停止而不丢失身份。稳定合同见 D51、[输出渠道](../../output-channels.md)。

证据：`publication-journal.test.ts` 的响应丢失、部分摘要、查询权限/分页/歧义、UUID
边界与租约故障；`auto-commit-runtime.test.ts` 的 SQLite 重启、租约接管及本地回执丢失；
三后端 conformance 的恢复预算和人工 Retry；`observability-api.test.ts` 的脱敏状态查询。
临时 Gitea 1.25.4 真实 issue 写入后丢弃响应，恢复查询确认仅一条 issue，8 项通过且服务清理。

最终门禁：WSL TLS/认证 PG、Redis 与独立 OOM 端点下 189 文件 / 4600 测试通过，
11 文件 / 21 测试按环境门控跳过；语句覆盖率 90.41%。lint/typecheck、275 文件 Markdown、
runtime build、6 个离线 eval 夹具、54 页文档站及链接/skill 元数据检查通过。
国内中科大镜像覆盖 Debian 主仓库和 security 源；仅在临时容器中引导 CA 后使用 HTTPS。
SVN 真实分析 2 项、最终 Gitea 8 项通过；WSL 部署夹具认证/权限/重启持久化通过。
本轮临时容器、命名卷、目录及备用本机 PG 均已清理，预存镜像和其他容器保留。
证据见 `build/logs/remote-reconciliation-*`。首次官方源下载缓慢中止、备用测试 helper
收尾错误的日志保留；正式国内镜像入口重跑全部门禁及清理成功。
不承诺 exactly-once；旧任务没有已保存身份、内存重启和旧写入器的边界保留。GitLab 固定版本
端到端生产验收仍在 Plan，其他远端协议故障由本地注入验证。未重复公网 GitHub/P4 或飞书通知。
双语文档、示例说明及输出维护 skill 已同步；配置与评审工具协议不变，无需修改 YAML
配置例或模型提示词。
