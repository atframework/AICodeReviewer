# M32 P4 提交者飞书归属（2026-09-22）

生产 CL 8215 的原始作者为 admin，提交 workspace 为 ultramanhu_PrxMain_WorkPC。
只读核查部署代码、配置、日志与真实成员目录：没有显式 user_mappings，
旧规则将 admin 匹配到 owent 的邮箱本地部分，提前返回；workspace 单独匹配
唯一指向 ultramanhu。该路径不调用身份模型。

P4 现在先匹配提交 workspace，再匹配账号邮箱和用户名；显式映射与黑名单
保留优先级，workspace 歧义阻止较弱规则/模型兜底。非 P4 及 guess_author=false
沿用原有合同。报告仍显示原始作者，通知使用 workspace 所属的目录成员。

修复前新增回归有 5 项失败，发布器请求中的卡片复现错误 @；修复后身份解析、飞书 API、
发布、身份模型与配置代次 5 文件共 100 项通过。同步双语指南、示例和 AI 合同，
独立身份提示词也明确共享账号不能覆盖 workspace 证据。

使用生产配置与实时目录，对部署版和修复版解析器做只读对比：owent → ultramanhu。
CL 8215 数据记录为 succeeded、2 个问题、1 条总结和 1 次发布。

最终全量发现 199 个文件，4409 项通过、66 项按外部端点环境条件跳过，零失败；
覆盖率原门槛通过。lint、typecheck、修改测试的额外严格类型检查、build、6 个离线
eval 夹具、273 文件 Markdown 与 docs:build 通过。额外类型检查暴露的两处既有
非空夹具声明已补齐，保留首轮失败日志；没有涉及 UI 或配置 schema，不适用浏览器
及 docs:check 门禁。原生测试服务退出后没有残留进程。

生产仅作只读诊断，未重放评审、发送消息或部署新版本。
本轮日志位于 build/logs/p4-mention-*。
