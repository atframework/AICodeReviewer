# M5 — 多 Agent CLI、Runtime Bundle 与 MCP 集成（已完成）

多 Agent CLI runtime bundle、manifest、原生 MCP 配置、stdout 修复/直连兜底及
MCP 状态文件读取已交付。默认 stdio，另有显式 Streamable HTTP transport。

证据：历史生产 Kilo sandbox 完整执行记录；本地 SDK client 测试覆盖 HTTP 工具发现、
调用、状态写入与 source context。实现/回归位于 agents、mcp-output 和 server 编排模块。
稳定合同见[架构](../architecture.md) §3.6.3、§3.7 及[输出合同](../../output-channels.md)。
其他 adapter 是否切换 HTTP 属集成选择，不是现行待办。
