# M12：GitHub App 原生认证

GitHub App schema、RS256 JWT/installation token 缓存刷新与三个 token 注入点已交付。
实现为 server/src/github-app-token.ts、bootstrap 及 webhook；包含 GHE 与凭据脱敏回归。

2026-08-28 签收生产 push 路径：atframework-aicr App 对三个仓库解析 token，
atsf4g-co 的 sample_solution 自然 push 完成验签→拉取→评审→managed issue 创建/关闭，
历史 issue #180–#183 等为证据。
pull_request 注入点只有单测，仍待自然 PR 验收，见[路线图](../../../Plan.md)。
稳定合同见[架构](../architecture.md) §3.2.1、决策 D32。
